/**
 * A tree/map of data used to determine the specific response when given an ActionType and Morality.
 *
 * @example ResponseMap --> Morality --> ActionType --> Spread of Responses.
 */
export enum ResponseSpreadType {
  STANDARD,
  POWERFUL,
}
