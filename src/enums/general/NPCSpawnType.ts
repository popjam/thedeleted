/** Relates to the method of spawning NPCs. */
export enum NPCSpawnType {
  /** Spawns the NPC away from the player, however makes sure they are still accessible. */
  ACCESSIBLE_TO_PLAYER_BUT_AVOID_PLAYER,
}
