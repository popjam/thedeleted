// const modVanilla = RegisterMod("test", 1);

// const mod = upgradeMod(modVanilla, [ ISCFeature.SAVE_DATA_MANAGER, ISCFeature.CUSTOM_HOTKEYS, ]
// as const);

// class Test extends ModFeature { v = { run: { test: 5, }, };

//   @Callback(ModCallback.POST_NEW_ROOM) postNewRoom(): void { print(this.v.run.test); } }

// new Test(mod);
