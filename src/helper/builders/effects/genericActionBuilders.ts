import { getRandomFromWeightedArray } from "isaacscript-common";
import type { Action } from "../../../classes/corruption/actions/Action";
import { DEFAULT_ACTION_WEIGHTS } from "../../../constants/corruptionConstants";

/** Will generate an Action based off default settings. */
// export function defaultActionBuilder( actionWeightings = DEFAULT_ACTION_WEIGHTS, ): Action {
// const actionType = getRandomFromWeightedArray(actionWeightings, undefined); }
